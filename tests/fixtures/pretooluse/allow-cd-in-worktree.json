{
  "description": "Bash cd command to path within own worktree should be allowed",
  "settings": {
    "permissions": {
      "allow": ["Bash(cd:*)"]
    }
  },
  "input": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "cd /test/agents/test-agent/repo/src",
      "description": "Change to src directory"
    },
    "cwd": "/test/agents/test-agent/repo"
  },
  "context": {
    "worktree_path": "/test/agents/test-agent/repo",
    "agent_dir": "/test/agents/test-agent",
    "agents_dir": "/test/agents",
    "root_repo": "/test/main-repo"
  },
  "expected": {
    "decision": "allow",
    "reason": "Tool in allow list, path in worktree"
  }
}

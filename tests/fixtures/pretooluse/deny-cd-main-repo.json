{
  "description": "Bash cd command to main repo should be denied",
  "settings": {
    "permissions": {
      "allow": ["Bash(cd:*)"]
    }
  },
  "input": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "cd /test/main-repo/src",
      "description": "Change to main repo src"
    },
    "cwd": "/test/agents/test-agent/repo"
  },
  "context": {
    "worktree_path": "/test/agents/test-agent/repo",
    "agent_dir": "/test/agents/test-agent",
    "agents_dir": "/test/agents",
    "root_repo": "/test/main-repo"
  },
  "expected": {
    "decision": "deny",
    "reason": "Access denied: work in your worktree, not the main repo"
  }
}
